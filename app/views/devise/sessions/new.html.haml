.contents.row
  .container
    %h2.login 続行するには our task にログインしてください
    .container__easy-login
      = link_to  = link_to users_guest_sign_in_path, method: :post, class: "easy-login-btn" do
        簡単ログイン
        %i.fas.fa-sign-in-alt
    .container__inner
      = form_with model: @user, url: user_session_path, id: 'new_user', class: 'new_user', local: true do |f|
        .field
          %em メールアドレス
          = f.email_field :email, autofocus: true
        .field
          %em パスワード
          = f.password_field :password, autocomplete: "off"
        - if devise_mapping.rememberable?
          .field
            %em.remember_me-label ログインを記録
            = f.check_box :remember_me, class: "remember_me"
        .actions
          = f.submit "ログイン"
        .no-accounts
          アカウントをお持ちではないでしょうか？
        .transition-signin
          = link_to "登録する", new_user_registration_path